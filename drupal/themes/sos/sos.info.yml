name: StaffOutsourcingSolutions
type: theme
description: This is a StaffOutsourcingSolutions
core_version_requirement: ^8 || ^9 || ^10
# Defines the base theme
base theme : false
package: custom
version: '1.0.0'
# Defines libraries group in which we can add CSS/JS.
libraries:
 - sos/global-styling
# Regions, quote the names
regions:
  header: 'Header'
  featured: 'Featured'
  primary_menu: 'Primary menu'
  secondary_menu: 'Secondary menu'
  sidebar_first: First sidebar
  content: 'Content'
  footer: 'Footer'